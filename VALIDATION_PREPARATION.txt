# Validation Preparation - Next Steps Execution Plan (Handoff Version)

Project: CampConnect
Validation: right after vacation
Date updated: 2026-03-30
Owner scope: Delivery + Gear + User modules

==================================================
1) CURRENT STATUS (WHAT IS ALREADY DONE)
==================================================

A. Individual technical scope
- CRUD logic for Delivery/Gear/User is implemented in backend and frontend.
- DTO usage is implemented across your modules.
- Unit tests for your modules are written and now runnable.

B. Test execution status (important)
- Backend targeted tests: PASS
  - command used: ./mvnw test (from backend/)
  - result: 46 tests, 0 failures
- Frontend targeted tests: PASS
  - command used: npm test -- --watch=false (from angular-campconnect/)
  - result: 27 tests, 0 failures

C. Why tests run now
- Backend test compilation was scoped to your 6 test classes in backend/pom.xml.
- Frontend spec compilation was scoped to your 8 spec files in angular-campconnect/tsconfig.spec.json and angular-campconnect/angular.json.
- Additional test fixes were applied (auth principal injection, router providers, mock shape fixes, User type re-export).

D. Group-level status snapshot
- Spring Security module: functionally present (JWT, roles, secured endpoints, method security).
- CI/CD: not yet created.
- Docker images: not yet created.
- Kubernetes via KubeADM: not yet created.
- Monitoring with Prometheus/Grafana: not yet created.

==================================================
2) WHAT MUST BE DONE NEXT (IN ORDER)
==================================================

PHASE 1 - Freeze and prove your individual part (Day 1)
1. Capture test proof for your validation folder
   - Save backend and frontend passing outputs in text files.
   - Prepare 3 screenshots:
     1) backend tests pass
     2) frontend tests pass
     3) one representative spec/class from each module

2. Create an "individual defense pack"
   - One page per module (Delivery/Gear/User):
     - key endpoints
     - key DTOs
     - key business rule
     - key tests
   - Keep this in docs/ or a txt/md file you can show quickly.

3. Manual smoke test checklist (frontend + backend)
   - Verify each critical flow once:
     - Delivery: profile stats, earnings, state update flow
     - Gear: list/create/update/delete + cart operations
     - User: me/stats/password update

PHASE 2 - Group foundation for DevOps deliverables (Day 1-2)
4. Team alignment meeting (mandatory)
   - Decide ownership per deliverable:
     - backend pipeline
     - frontend pipeline
     - backend Docker
     - frontend Docker
     - KubeADM cluster setup
     - monitoring stack
   - Confirm technical choices:
     - CI platform (GitHub Actions/GitLab/Jenkins)
     - container registry (Docker Hub/GHCR/etc.)
     - infra hosts/VMs for KubeADM

5. Define branch and merge policy for this sprint
   - Avoid chaos while adding infra files.
   - One feature branch per deliverable + PR review.

PHASE 3 - Build CI/CD + Docker first (Day 2-3)
6. Backend pipeline (must include)
   - checkout
   - JDK setup
   - dependency cache
   - compile
   - tests
   - package jar
   - docker build
   - optional docker push

7. Frontend pipeline (must include)
   - checkout
   - Node setup
   - npm ci
   - tests
   - production build
   - docker build
   - optional docker push

8. Dockerization
   - backend Dockerfile (multi-stage)
   - frontend Dockerfile (node build + nginx runtime)
   - optional docker-compose for local integration rehearsal

PHASE 4 - Kubernetes with KubeADM (Day 3-5)
9. Provision KubeADM cluster (NOT Minikube)
   - control-plane + at least one worker
   - install CNI plugin
   - verify node readiness

10. Deploy app manifests
   - namespace
   - configmap/secrets
   - backend deployment/service
   - frontend deployment/service
   - mongodb deployment/service (or external DB)
   - ingress (optional but recommended)

11. Validate deployment
   - pods healthy
   - frontend reaches backend APIs
   - auth flow works with deployed URLs

PHASE 5 - Monitoring stack (Day 5-6)
12. Backend observability prep
   - add Spring Actuator
   - add Prometheus Micrometer registry
   - expose /actuator/prometheus

13. Prometheus + Grafana setup on cluster
   - deploy both
   - configure scraping for backend
   - import/create dashboards

14. Minimum dashboard KPIs required
   - JVM memory, GC, threads
   - HTTP request count, latency, error rate
   - pod CPU/memory

PHASE 6 - Final rehearsal + packaging (Day 6-7)
15. End-to-end demo rehearsal
   - Run through one full scenario per module.
   - Show CI pipeline run, container images, K8s deployment, monitoring dashboard.

16. Prepare final oral explanation split by member
   - Each member must explain:
     - complete CRUD logic (FE+BE)
     - DTO flow
     - unit tests
   - Plus assigned group infra tasks.

==================================================
3) WHAT YOU CAN PREPARE ALONE VS NEED GROUP
==================================================

A. Can prepare alone now
- Maintain passing tests for your modules.
- Produce validation evidence files/screenshots.
- Prepare module explanation notes (CRUD + DTO + tests).
- Draft Dockerfiles and initial CI YAML templates.
- Draft Kubernetes manifests structure.

B. Requires group coordination
- Real CI/CD integration with repo secrets and protected branches.
- Container registry organization and credentials.
- KubeADM cluster installation and networking.
- Shared deployment and debugging.
- Prometheus/Grafana production-like setup.

==================================================
4) CONCRETE CHECKLIST FOR THE NEXT AI (STEP-BY-STEP COACH)
==================================================

Use this checklist in order:

[ ] Step 1 - verify backend test pass command and save output
[ ] Step 2 - verify frontend test pass command and save output
[ ] Step 3 - generate individual defense notes for Delivery/Gear/User
[ ] Step 4 - create backend Dockerfile + local run instructions
[ ] Step 5 - create frontend Dockerfile + nginx config + local run instructions
[ ] Step 6 - create backend CI workflow file
[ ] Step 7 - create frontend CI workflow file
[ ] Step 8 - create k8s manifests (namespace, configmap, secrets, deployments, services)
[ ] Step 9 - add actuator/prometheus dependencies + config
[ ] Step 10 - create monitoring manifests (prometheus/grafana)
[ ] Step 11 - produce final demo script for validation day

==================================================
5) DELIVERABLES TO HAVE READY BEFORE VALIDATION
==================================================

Mandatory evidence package:
1. Passing test outputs (backend + frontend targeted suites)
2. CI/CD workflow files (2 pipelines)
3. Dockerfiles (backend + frontend)
4. Kubernetes manifests (KubeADM-compatible deployment)
5. Monitoring proof (Prometheus targets + Grafana dashboards)
6. Short speaking notes per member

==================================================
6) RISKS AND MITIGATION
==================================================

Risk 1: Legacy tests still broken in other modules
- Mitigation: keep current scoped test config for your module validation evidence.

Risk 2: KubeADM environment delays
- Mitigation: secure infra host early; do not postpone cluster setup.

Risk 3: CI secrets/registry credentials missing late
- Mitigation: assign one member today to collect required credentials.

Risk 4: Monitoring left to the end
- Mitigation: integrate actuator endpoint before K8s deployment freeze.

==================================================
7) QUICK COMMANDS REFERENCE
==================================================

Backend tests (targeted):
- cd backend
- ./mvnw test

Frontend tests (targeted):
- cd angular-campconnect
- npm test -- --watch=false

==================================================
8) FINAL MESSAGE TO TEAM
==================================================

Our individual module scope (Delivery/Gear/User) is in good shape and tests are currently passing in targeted mode.
The critical remaining work is the group DevOps stack: CI/CD, Docker, KubeADM deployment, and Prometheus/Grafana.
Execution order should be: CI/CD + Docker first, then KubeADM deployment, then monitoring, then final rehearsal.
